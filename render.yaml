services:
  - type: web
    name: faroll-publici
    runtime: node
    plan: starter          # Starter ($7/mês) — necessário para disco persistente
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: ENCRYPTION_KEY
        generateValue: true  # Render gera automaticamente
      - key: DB_PATH
        value: /data/faroll.db
    disk:
      name: faroll-data
      mountPath: /data
      sizeGB: 1
